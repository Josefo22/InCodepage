/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    swcMinify: true,
    // Agrega aquí cualquier configuración adicional que uses
  };
  
  export default nextConfig;
  